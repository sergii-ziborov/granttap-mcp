# Cloud approvals

This feature module accesses the relay's private Web approval endpoint with the pairing-scoped `pushAuth` capability. HTTP cancellation and bounded response parsing are kept separate from approval-card policy.
