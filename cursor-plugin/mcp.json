{
  "mcpServers": {
    "granttap": {
      "command": "node",
      "args": [
        "-e",
        "const {spawn}=require('child_process');const {existsSync}=require('fs');const {dirname,join}=require('path');const P='granttap-mcp@0.8.18';const w=process.platform==='win32';const sib=n=>{const c=join(dirname(process.execPath),n);return existsSync(c)?c:''};const g=w?sib('granttap-mcp.cmd')||sib('granttap-mcp'):sib('granttap-mcp');const n=w?sib('npx.cmd')||sib('npx'):sib('npx');const command=w?(process.env.ComSpec||'cmd.exe'):(g||n||'npx');const args=w?['/d','/s','/c',g?`\"${g}\"`:`where granttap-mcp >nul 2>nul && granttap-mcp || \"${n||'npx'}\" -y ${P}`]:g?[]:['-y',P];const child=spawn(command,args,{stdio:'inherit',windowsHide:true});child.on('error',e=>{process.stderr.write('[granttap] '+e.message+'\\n');process.exit(1)});child.on('exit',c=>process.exit(c??1));"
      ]
    }
  }
}
