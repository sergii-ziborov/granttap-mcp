---
description: The agent connects this computer to GrantTap
---

Call GrantTap tools yourself. Do not ask the person to pair in this chat.

1. Call `connection_status`.
2. If this computer is not paired, call `connect`.
3. Do not print a pairing URI, token, or QR in chat.
4. The GrantTap connection card shows the device list and one-time QR when
   Cursor supports interactive MCP Apps. `granttap.com/connect` approves the
   coding app but does not display a pairing QR. If the card cannot call tools,
   tell the person to run `granttap phone add` in their own terminal.
